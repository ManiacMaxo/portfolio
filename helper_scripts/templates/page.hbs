import React from 'react'
import { Layout } from '../components'

const {{filename}} = (): JSX.Element => {
    return <Layout title='{{filename}}'>Hello {{filename}}</Layout>
}

export default {{filename}}